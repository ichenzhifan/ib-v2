import React, { Component, PropTypes } from 'react';
import XModal from "../../../../common/ZNOComponents/XModal";
import XButton from "../../../../common/ZNOComponents/XButton";
import { translate } from "react-translate";
import ItemList from "../ItemList";
import './index.scss';
import close from './close.png';

class UploadModal extends Component {
	constructor(props) {
    super(props);
    this.state = {
      uploadList : [
        {
          'filename' : 'img-0',
          'percent' : 20
        },
        {
          'filename' : 'img-1',
          'percent' : 50
        },
        {
          'filename' : 'img-2',
          'percent' : 10
        }
      ]
    }
	}

  render() {
    const { onClosed, opened, t } = this.props;
    return (
      <XModal className="upload-modal"
              onClosed={ onClosed }
              opened={ opened }>
          <div className="box-image-upload">
            <h3 className="title">
              { t('UPLOAD_IMAGES') }
            </h3>
            <ItemList uploadList={ this.state.uploadList } />
            <div className="upload-meta">
              <div className="upload-info">
                <span className="compelete">
                  { t('COMPLETE_COUNT',{n:0}) }
                </span>
                <span className="failed">
                  { t('FILED_COUNT',{n:0}) }
                </span>
              </div>
              <div className="upload-buttons">
                <XButton onClick={ this.handleAddMore.bind(this) }
                         className="white">
                  { t('ADD_MORE_PHOTOS') }
                </XButton>
                <XButton onClick={ this.handleCancelAll.bind(this) }
                         className="cancel-all">
                  { t('CANCELL_ALL') }
                </XButton>
              </div>
            </div>
          </div>
        </XModal>
    );
  }

  handleAddMore(e) {
    //
  }

  handleCancelAll(e) {
    //
  }

  selectFile(){

  }
}

UploadModal.propTypes = {
  onClick: PropTypes.func.isRequired,
  className: PropTypes.string,
  width: PropTypes.number,
  height: PropTypes.number
};

export default translate('UploadModal')(UploadModal);
