import React, { Component, PropTypes } from 'react';
import XProgress from "../../../../common/ZNOComponents/XProgress";
import './index.scss';

class ItemList extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    const { uploadItem } = this.props;

    return (
      <li>
        <div className="file-name">
          { uploadItem.name }
        </div>
        <div className="file-progress">
          <XProgress percent={ uploadItem.percent } />
          <span className="status">{ uploadItem.percent } { uploadItem.subfix }</span>
          <a href="javascript:void(0);"
             className="delete">
            <img width="8px"
                 height="8px"
                 src={ close }
                 alt="Delete" />
          </a>
          <a href="javascript:void(0);"
             className="retry">
             Retry
          </a>
        </div>
      </li>
    );
  }
}

ItemList.propTypes = {
  uploadItem: PropTypes.object.isRequired,
};

export default ItemList;
