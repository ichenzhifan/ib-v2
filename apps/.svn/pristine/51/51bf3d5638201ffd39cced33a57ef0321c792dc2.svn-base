import React, { Component, PropTypes } from 'react';
import { merge, get, set, pick, forEach, isEmpty } from 'lodash';
import XModal from '../../../../common/ZNOComponents/XModal';
import XButton from '../../../../common/ZNOComponents/XButton';
import { translate } from 'react-translate';
import ItemList from '../ItemList';
import './index.scss';
import close from './close.png';

class UploadModal extends Component {
	constructor(props) {
  super(props);
}

  componentWillReceiveProps(nextProps) {

  }

  handleAddMore(e) {
    //
  }

  handleCancelAll(e) {
    //
  }

  selectFile() {

  }

  render() {
    const { onClosed, opened, t, uploadImages } = this.props;
    return (
      <XModal
        className="upload-modal"
        onClosed={onClosed}
        opened={opened}
      >
        <div className="box-image-upload">
          <h3 className="title">
            { t('UPLOAD_IMAGES') }
          </h3>
          <ItemList uploadList={ uploadImages } />
          <div className="upload-meta">
            <div className="upload-info">
              <span className="compelete">
                { t('COMPLETE_COUNT', { n: 0 }) }
              </span>
              <span className="failed">
                { t('FILED_COUNT', { n: 0 }) }
              </span>
            </div>
            <div className="upload-buttons">
              <XButton
                onClicked={ this.handleAddMore.bind(this) }
                className="white"
              >
                { t('ADD_MORE_PHOTOS') }
              </XButton>
              <XButton
                onClicked={this.handleCancelAll.bind(this)}
                className="cancel-all"
              >
                { t('CANCELL_ALL') }
              </XButton>
            </div>
          </div>
        </div>
      </XModal>
    );
  }
}

UploadModal.propTypes = {
  className: PropTypes.string,
  width: PropTypes.number,
  height: PropTypes.number,
  uploadList : PropTypes.array
};

export default translate('UploadModal')(UploadModal);
