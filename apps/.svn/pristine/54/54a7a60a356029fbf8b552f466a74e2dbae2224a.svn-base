import React, { Component, PropTypes } from 'react';
import XFileUpload from '../../../../common/ZNOComponents/XFileUpload';
import XButton from '../../../../common/ZNOComponents/XButton';
import ListTab from '../ListTab';
import ImageList from '../ImageList';
import ImageItem from '../ImageItem';
import './index.scss';

class SideBar extends Component {
  constructor(props) {
    super(props);

    this.state = {
      selectedSize: null,
    };

  }

  render() {
    const { hideUsedToggle, optionChange, onSelected,uploadedImagesActions, toggleModal } = this.props;

    return (
      <aside className="side-bar">

        <ListTab className="list-tab">Images</ListTab>

        <XFileUpload className="add-photo" uploadedImagesActions={ uploadedImagesActions } toggleModal={ toggleModal } multiple="multiple">Add Photo</XFileUpload>

        <ImageList> </ImageList>

      </aside>
    );
  }
}

export default SideBar;

